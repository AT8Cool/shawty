services:
  redis:
    image: redis:latest
    restart: unless-stopped
   

  backend:
    build: .
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      REDIS_HOST: redis
      REDIS_PORT: 6379
    depends_on:
      - redis

  frontend:
    build: ./frontend
    restart: unless-stopped
    ports:
      - "4173:4173"
    depends_on:
      - backend