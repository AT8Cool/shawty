
  # Colorful URL Shortener SPA

  This is a code bundle for Colorful URL Shortener SPA. The original project is available at https://www.figma.com/design/8gBkmEGxhp3ouUbPYjiUUQ/Colorful-URL-Shortener-SPA.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  